# Pack It Up — Replit notes

**Read `AGENTS.md` first** — it is the single full copy of the project architecture, section map, and hard rules. This file only adds Replit-specific bits.

Replit is not currently an active team in the AI dev team (see `docs/ai-team/README.md`). If you are an agent running in Replit: follow `AGENTS.md`, work on a branch (never directly on `main`), and close out per `docs/ai-team/end-here.md`.

## Running the game in Replit

```bash
pnpm --filter @workspace/pack-it-up run dev
```

Game at `/` · layout editor at `?edit=1` · sprite preview at `?preview=bed`.
